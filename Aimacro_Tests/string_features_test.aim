# =============================================================================
# TEST: string_features_test.aim
# Testing core string features that may or may not work
# =============================================================================

def main():
    print("=== STRING FEATURES TEST ===")
    print("")
    
    # -----------------------------------------
    # TEST 1: String Equality (==)
    # -----------------------------------------
    print("TEST 1: String Equality (==)")
    name = "Claude"
    if name == "Claude":
        print("PASS: name == Claude")
    else:
        print("FAIL: name == Claude not detected")
    end
    
    other = "Bob"
    if other == "Claude":
        print("FAIL: Bob should not equal Claude")
    else:
        print("PASS: Bob != Claude")
    end
    print("")
    
    # -----------------------------------------
    # TEST 2: String Inequality (!=)
    # -----------------------------------------
    print("TEST 2: String Inequality (!=)")
    if name != "Bob":
        print("PASS: Claude != Bob")
    else:
        print("FAIL: Claude should != Bob")
    end
    
    if name != "Claude":
        print("FAIL: Claude should == Claude")
    else:
        print("PASS: Claude == Claude (via !=)")
    end
    print("")
    
    # -----------------------------------------
    # TEST 3: str() Conversion
    # -----------------------------------------
    print("TEST 3: str() Conversion")
    count = 42
    count_str = str(count)
    print("Converted number to string")
    
    # Try concatenating with str()
    msg = "The answer is: " + str(count)
    print(msg)
    
    # Inline str() in concat
    print("Value: " + str(100))
    print("")
    
    # -----------------------------------------
    # TEST 4: len() on Strings
    # -----------------------------------------
    print("TEST 4: len() on Strings")
    text = "Hello"
    text_len = len(text)
    print("Length of Hello:")
    print(text_len)
    
    empty = ""
    print("Length of empty string:")
    print(len(empty))
    
    long_text = "This is a longer string"
    print("Length of longer string:")
    print(len(long_text))
    print("")
    
    # -----------------------------------------
    # TEST 5: Combined - Real World Example
    # -----------------------------------------
    print("TEST 5: Combined Example")
    username = "admin"
    
    if username == "admin":
        print("Welcome, administrator!")
        msg = "User " + username + " logged in"
        print(msg)
    end
    
    password = "secret123"
    pass_len = len(password)
    if pass_len < 8:
        print("Password too short!")
    else:
        print("Password length OK: " + str(pass_len))
    end
    print("")
    
    print("=== ALL TESTS COMPLETE ===")
end