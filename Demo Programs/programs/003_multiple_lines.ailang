// 003 — Print multiple lines
// Each PrintMessage writes its bytes directly to stdout; the trailing \n
// creates the line break. No buffering between calls.
SubRoutine.Main {
    PrintMessage("Line one\n")
    PrintMessage("Line two\n")
    PrintMessage("Line three\n")
}
RunTask(Main)
