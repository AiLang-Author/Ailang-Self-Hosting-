// Library.JSOop.ailang — JavaScript Object-Oriented Programming Support
// Forked from Library.OOP.ailang for JS engine use (prototype chains, constructors,
// instanceof, getter/setter descriptors, Object.create, etc.)
//
// All symbols renamed from OOP.* -> JSOop.* to avoid collisions with base library.
// Will be extended with JS-specific semantics as needed.
//
// Copyright (c) 2026 Sean Collins, 2 Paws Machine and Engineering. SCSL.

LibraryImport.Arrays
LibraryImport.Array
LibraryImport.Hash

//=============================================================================
// INTERNAL DATA STRUCTURES
//=============================================================================

// Global registry of all defined classes
// Key: class_name (String)
// Value: ClassDef hash
FixedPool.JSClassRegistry {
    "registry": Initialize=0
    "initialized": Initialize=0
}

// ClassDef structure (stored in registry as hash):
//   "name": String - class name
//   "parent": String|Null - parent class name (prototype chain)
//   "fields": Hash - field_name -> field_type
//   "methods": Hash - method_name -> function_address
//   "field_defaults": Hash - field_name -> default_value
//   "getters": Hash - property_name -> getter_function_address
//   "setters": Hash - property_name -> setter_function_address

// Object structure (returned by JSOop.ObjectNew as hash):
//   "__class__": String - class name (constructor.name)
//   "__data__": Hash - field_name -> current_value
//   "__proto__": Address - prototype chain link


//=============================================================================
// REGISTRY INITIALIZATION
//=============================================================================

Function.JSOop.EnsureInit {
    Output: Integer
    Body: {
        IfCondition EqualTo(JSClassRegistry.initialized, 0) ThenBlock: {
            JSClassRegistry.registry = Hash.Create(256)
            JSClassRegistry.initialized = 1
        }
        ReturnValue(1)
    }
}


//=============================================================================
// CLASS DEFINITION
//=============================================================================

Function.JSOop.ClassDefine {
    Input: name: Address
    Input: parent: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        existing = Hash.HGet(JSClassRegistry.registry, name)
        IfCondition NotEqual(existing, 0) ThenBlock: {
            ReturnValue(0)
        }

        class_def = Hash.Create(64)
        Hash.HSet(class_def, "name", name)
        Hash.HSet(class_def, "parent", parent)
        Hash.HSet(class_def, "fields", Hash.Create(32))
        Hash.HSet(class_def, "methods", Hash.Create(32))
        Hash.HSet(class_def, "field_defaults", Hash.Create(32))
        Hash.HSet(class_def, "getters", Hash.Create(16))
        Hash.HSet(class_def, "setters", Hash.Create(16))

        IfCondition NotEqual(parent, 0) ThenBlock: {
            parent_def = Hash.HGet(JSClassRegistry.registry, parent)
            IfCondition EqualTo(parent_def, 0) ThenBlock: {
                ReturnValue(0)
            }

            parent_fields = Hash.HGet(parent_def, "fields")
            parent_field_keys = Hash.Keys(parent_fields)
            num_parent_fields = Array.Size(parent_field_keys)

            i = 0
            WhileLoop LessThan(i, num_parent_fields) {
                field_key = Array.Get(parent_field_keys, i)
                field_type = Hash.HGet(parent_fields, field_key)
                Hash.HSet(Hash.HGet(class_def, "fields"), field_key, field_type)
                i = Add(i, 1)
            }
            Array.Destroy(parent_field_keys)

            parent_defaults = Hash.HGet(parent_def, "field_defaults")
            parent_default_keys = Hash.Keys(parent_defaults)
            num_defaults = Array.Size(parent_default_keys)

            j = 0
            WhileLoop LessThan(j, num_defaults) {
                default_key = Array.Get(parent_default_keys, j)
                default_val = Hash.HGet(parent_defaults, default_key)
                Hash.HSet(Hash.HGet(class_def, "field_defaults"), default_key, default_val)
                j = Add(j, 1)
            }
            Array.Destroy(parent_default_keys)

            parent_methods = Hash.HGet(parent_def, "methods")
            parent_method_keys = Hash.Keys(parent_methods)
            num_methods = Array.Size(parent_method_keys)

            k = 0
            WhileLoop LessThan(k, num_methods) {
                method_key = Array.Get(parent_method_keys, k)
                method_addr = Hash.HGet(parent_methods, method_key)
                Hash.HSet(Hash.HGet(class_def, "methods"), method_key, method_addr)
                k = Add(k, 1)
            }
            Array.Destroy(parent_method_keys)

            // Inherit getters
            parent_getters = Hash.HGet(parent_def, "getters")
            IfCondition NotEqual(parent_getters, 0) ThenBlock: {
                parent_getter_keys = Hash.Keys(parent_getters)
                num_getters = Array.Size(parent_getter_keys)
                g = 0
                WhileLoop LessThan(g, num_getters) {
                    getter_key = Array.Get(parent_getter_keys, g)
                    getter_addr = Hash.HGet(parent_getters, getter_key)
                    Hash.HSet(Hash.HGet(class_def, "getters"), getter_key, getter_addr)
                    g = Add(g, 1)
                }
                Array.Destroy(parent_getter_keys)
            }

            // Inherit setters
            parent_setters = Hash.HGet(parent_def, "setters")
            IfCondition NotEqual(parent_setters, 0) ThenBlock: {
                parent_setter_keys = Hash.Keys(parent_setters)
                num_setters = Array.Size(parent_setter_keys)
                s = 0
                WhileLoop LessThan(s, num_setters) {
                    setter_key = Array.Get(parent_setter_keys, s)
                    setter_addr = Hash.HGet(parent_setters, setter_key)
                    Hash.HSet(Hash.HGet(class_def, "setters"), setter_key, setter_addr)
                    s = Add(s, 1)
                }
                Array.Destroy(parent_setter_keys)
            }
        }

        Hash.HSet(JSClassRegistry.registry, name, class_def)
        ReturnValue(1)
    }
}

Function.JSOop.ClassField {
    Input: class_name: Address
    Input: field_name: Address
    Input: field_type: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        fields = Hash.HGet(class_def, "fields")
        Hash.HSet(fields, field_name, field_type)
        ReturnValue(1)
    }
}

Function.JSOop.ClassFieldDefault {
    Input: class_name: Address
    Input: field_name: Address
    Input: default_val: Integer
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        field_defaults = Hash.HGet(class_def, "field_defaults")
        Hash.HSet(field_defaults, field_name, default_val)
        ReturnValue(1)
    }
}

Function.JSOop.ClassMethod {
    Input: class_name: Address
    Input: method_name: Address
    Input: func_addr: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        methods = Hash.HGet(class_def, "methods")
        Hash.HSet(methods, method_name, func_addr)
        ReturnValue(1)
    }
}


//=============================================================================
// GETTER / SETTER SUPPORT (JS-specific extension)
//=============================================================================

Function.JSOop.ClassGetter {
    Input: class_name: Address
    Input: prop_name: Address
    Input: func_addr: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        getters = Hash.HGet(class_def, "getters")
        Hash.HSet(getters, prop_name, func_addr)
        ReturnValue(1)
    }
}

Function.JSOop.ClassSetter {
    Input: class_name: Address
    Input: prop_name: Address
    Input: func_addr: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        setters = Hash.HGet(class_def, "setters")
        Hash.HSet(setters, prop_name, func_addr)
        ReturnValue(1)
    }
}


//=============================================================================
// MIXIN SUPPORT
//=============================================================================

Function.JSOop.ClassMixin {
    Input: target_class: Address
    Input: mixin_class: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        target_def = Hash.HGet(JSClassRegistry.registry, target_class)
        mixin_def = Hash.HGet(JSClassRegistry.registry, mixin_class)

        IfCondition EqualTo(target_def, 0) ThenBlock: {
            ReturnValue(0)
        }
        IfCondition EqualTo(mixin_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        target_methods = Hash.HGet(target_def, "methods")
        mixin_methods = Hash.HGet(mixin_def, "methods")
        mixin_method_keys = Hash.Keys(mixin_methods)
        num_mixin_methods = Array.Size(mixin_method_keys)

        i = 0
        WhileLoop LessThan(i, num_mixin_methods) {
            method_key = Array.Get(mixin_method_keys, i)
            existing = Hash.HGet(target_methods, method_key)
            IfCondition EqualTo(existing, 0) ThenBlock: {
                method_addr = Hash.HGet(mixin_methods, method_key)
                Hash.HSet(target_methods, method_key, method_addr)
            }
            i = Add(i, 1)
        }
        Array.Destroy(mixin_method_keys)

        ReturnValue(1)
    }
}

Function.JSOop.ClassMixinOverride {
    Input: target_class: Address
    Input: mixin_class: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        target_def = Hash.HGet(JSClassRegistry.registry, target_class)
        mixin_def = Hash.HGet(JSClassRegistry.registry, mixin_class)

        IfCondition EqualTo(target_def, 0) ThenBlock: {
            ReturnValue(0)
        }
        IfCondition EqualTo(mixin_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        target_methods = Hash.HGet(target_def, "methods")
        mixin_methods = Hash.HGet(mixin_def, "methods")
        mixin_method_keys = Hash.Keys(mixin_methods)
        num_mixin_methods = Array.Size(mixin_method_keys)

        i = 0
        WhileLoop LessThan(i, num_mixin_methods) {
            method_key = Array.Get(mixin_method_keys, i)
            method_addr = Hash.HGet(mixin_methods, method_key)
            Hash.HSet(target_methods, method_key, method_addr)
            i = Add(i, 1)
        }
        Array.Destroy(mixin_method_keys)

        ReturnValue(1)
    }
}


//=============================================================================
// OBJECT INSTANTIATION
//=============================================================================

Function.JSOop.ObjectNew {
    Input: class_name: Address
    Output: Address
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        obj = Hash.Create(32)
        Hash.HSet(obj, "__class__", class_name)
        Hash.HSet(obj, "__data__", Hash.Create(32))
        Hash.HSet(obj, "__proto__", 0)

        data = Hash.HGet(obj, "__data__")
        fields = Hash.HGet(class_def, "fields")
        field_defaults = Hash.HGet(class_def, "field_defaults")
        field_keys = Hash.Keys(fields)
        num_fields = Array.Size(field_keys)

        i = 0
        WhileLoop LessThan(i, num_fields) {
            field_name = Array.Get(field_keys, i)

            default_val = Hash.HGet(field_defaults, field_name)
            IfCondition NotEqual(default_val, 0) ThenBlock: {
                Hash.HSet(data, field_name, default_val)
            } ElseBlock: {
                field_type = Hash.HGet(fields, field_name)
                Hash.HSet(data, field_name, 0)
            }
            i = Add(i, 1)
        }
        Array.Destroy(field_keys)

        ReturnValue(obj)
    }
}

Function.JSOop.ObjectNewInit {
    // Create instance AND call constructor (__init__) with args
    Input: class_name: Address
    Input: init_args: Address
    Output: Address
    Body: {
        obj = JSOop.ObjectNew(class_name)
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }

        IfCondition JSOop.MethodExists(obj, "__init__") ThenBlock: {
            JSOop.MethodCall(obj, "__init__", init_args)
        }

        ReturnValue(obj)
    }
}


//=============================================================================
// FIELD ACCESS
//=============================================================================

Function.JSOop.ObjectGet {
    Input: obj: Address
    Input: field_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        data = Hash.HGet(obj, "__data__")
        ReturnValue(Hash.HGet(data, field_name))
    }
}

Function.JSOop.ObjectSet {
    Input: obj: Address
    Input: field_name: Address
    Input: value: Integer
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        data = Hash.HGet(obj, "__data__")
        Hash.HSet(data, field_name, value)
        ReturnValue(1)
    }
}

Function.JSOop.ObjectHasField {
    Input: obj: Address
    Input: field_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        data = Hash.HGet(obj, "__data__")
        value = Hash.HGet(data, field_name)
        IfCondition NotEqual(value, 0) ThenBlock: {
            ReturnValue(1)
        }
        keys = Hash.Keys(data)
        num_keys = Array.Size(keys)
        found = 0
        i = 0
        WhileLoop LessThan(i, num_keys) {
            key = Array.Get(keys, i)
            cmp = StringCompare(key, field_name)
            IfCondition EqualTo(cmp, 0) ThenBlock: {
                found = 1
            }
            i = Add(i, 1)
        }
        Array.Destroy(keys)
        ReturnValue(found)
    }
}


//=============================================================================
// METHOD DISPATCH
//=============================================================================

Function.JSOop._MethodLookupInClass {
    Input: class_name: Address
    Input: method_name: Address
    Output: Address
    Body: {
        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }
        methods = Hash.HGet(class_def, "methods")
        ReturnValue(Hash.HGet(methods, method_name))
    }
}

Function.JSOop._MethodLookupWithInheritance {
    Input: class_name: Address
    Input: method_name: Address
    Output: Address
    Body: {
        current_class = class_name

        WhileLoop NotEqual(current_class, 0) {
            func_addr = JSOop._MethodLookupInClass(current_class, method_name)
            IfCondition NotEqual(func_addr, 0) ThenBlock: {
                ReturnValue(func_addr)
            }

            class_def = Hash.HGet(JSClassRegistry.registry, current_class)
            IfCondition EqualTo(class_def, 0) ThenBlock: {
                ReturnValue(0)
            }
            current_class = Hash.HGet(class_def, "parent")
        }

        ReturnValue(0)
    }
}

Function.JSOop.MethodCall {
    Input: obj: Address
    Input: method_name: Address
    Input: args: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }

        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._MethodLookupWithInheritance(class_name, method_name)

        IfCondition EqualTo(func_addr, 0) ThenBlock: {
            ReturnValue(0)
        }

        num_args = Array.Size(args)

        IfCondition EqualTo(num_args, 0) ThenBlock: {
            result = CallIndirect(func_addr, obj)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 1) ThenBlock: {
            arg0 = Array.Get(args, 0)
            result = CallIndirect(func_addr, obj, arg0)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 2) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            result = CallIndirect(func_addr, obj, arg0, arg1)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 3) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            arg2 = Array.Get(args, 2)
            result = CallIndirect(func_addr, obj, arg0, arg1, arg2)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 4) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            arg2 = Array.Get(args, 2)
            arg3 = Array.Get(args, 3)
            result = CallIndirect(func_addr, obj, arg0, arg1, arg2, arg3)
            ReturnValue(result)
        }

        // 5+ args
        arg0 = Array.Get(args, 0)
        arg1 = Array.Get(args, 1)
        arg2 = Array.Get(args, 2)
        arg3 = Array.Get(args, 3)
        arg4 = Array.Get(args, 4)
        result = CallIndirect(func_addr, obj, arg0, arg1, arg2, arg3, arg4)
        ReturnValue(result)
    }
}

Function.JSOop.MethodExists {
    Input: obj: Address
    Input: method_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }

        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._MethodLookupWithInheritance(class_name, method_name)

        IfCondition NotEqual(func_addr, 0) ThenBlock: {
            ReturnValue(1)
        }
        ReturnValue(0)
    }
}

Function.JSOop.MethodLookup {
    Input: obj: Address
    Input: method_name: Address
    Output: Address
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }

        class_name = Hash.HGet(obj, "__class__")
        ReturnValue(JSOop._MethodLookupWithInheritance(class_name, method_name))
    }
}


//=============================================================================
// GETTER / SETTER DISPATCH (JS-specific)
//=============================================================================

Function.JSOop._GetterLookup {
    Input: class_name: Address
    Input: prop_name: Address
    Output: Address
    Body: {
        current_class = class_name
        WhileLoop NotEqual(current_class, 0) {
            class_def = Hash.HGet(JSClassRegistry.registry, current_class)
            IfCondition EqualTo(class_def, 0) ThenBlock: {
                ReturnValue(0)
            }
            getters = Hash.HGet(class_def, "getters")
            IfCondition NotEqual(getters, 0) ThenBlock: {
                func_addr = Hash.HGet(getters, prop_name)
                IfCondition NotEqual(func_addr, 0) ThenBlock: {
                    ReturnValue(func_addr)
                }
            }
            current_class = Hash.HGet(class_def, "parent")
        }
        ReturnValue(0)
    }
}

Function.JSOop._SetterLookup {
    Input: class_name: Address
    Input: prop_name: Address
    Output: Address
    Body: {
        current_class = class_name
        WhileLoop NotEqual(current_class, 0) {
            class_def = Hash.HGet(JSClassRegistry.registry, current_class)
            IfCondition EqualTo(class_def, 0) ThenBlock: {
                ReturnValue(0)
            }
            setters = Hash.HGet(class_def, "setters")
            IfCondition NotEqual(setters, 0) ThenBlock: {
                func_addr = Hash.HGet(setters, prop_name)
                IfCondition NotEqual(func_addr, 0) ThenBlock: {
                    ReturnValue(func_addr)
                }
            }
            current_class = Hash.HGet(class_def, "parent")
        }
        ReturnValue(0)
    }
}

Function.JSOop.GetterCall {
    // Call a getter on an object — returns the property value
    Input: obj: Address
    Input: prop_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._GetterLookup(class_name, prop_name)
        IfCondition EqualTo(func_addr, 0) ThenBlock: {
            ReturnValue(0)
        }
        result = CallIndirect(func_addr, obj)
        ReturnValue(result)
    }
}

Function.JSOop.SetterCall {
    // Call a setter on an object — sets the property value
    Input: obj: Address
    Input: prop_name: Address
    Input: value: Integer
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._SetterLookup(class_name, prop_name)
        IfCondition EqualTo(func_addr, 0) ThenBlock: {
            ReturnValue(0)
        }
        result = CallIndirect(func_addr, obj, value)
        ReturnValue(result)
    }
}

Function.JSOop.HasGetter {
    Input: obj: Address
    Input: prop_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._GetterLookup(class_name, prop_name)
        IfCondition NotEqual(func_addr, 0) ThenBlock: {
            ReturnValue(1)
        }
        ReturnValue(0)
    }
}

Function.JSOop.HasSetter {
    Input: obj: Address
    Input: prop_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        class_name = Hash.HGet(obj, "__class__")
        func_addr = JSOop._SetterLookup(class_name, prop_name)
        IfCondition NotEqual(func_addr, 0) ThenBlock: {
            ReturnValue(1)
        }
        ReturnValue(0)
    }
}


//=============================================================================
// INTROSPECTION
//=============================================================================

Function.JSOop.ObjectClass {
    Input: obj: Address
    Output: Address
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }
        ReturnValue(Hash.HGet(obj, "__class__"))
    }
}

Function.JSOop.ObjectIsInstance {
    Input: obj: Address
    Input: class_name: Address
    Output: Integer
    Body: {
        IfCondition EqualTo(obj, 0) ThenBlock: {
            ReturnValue(0)
        }

        obj_class = Hash.HGet(obj, "__class__")
        current_class = obj_class

        WhileLoop NotEqual(current_class, 0) {
            cmp = StringCompare(current_class, class_name)
            IfCondition EqualTo(cmp, 0) ThenBlock: {
                ReturnValue(1)
            }

            class_def = Hash.HGet(JSClassRegistry.registry, current_class)
            IfCondition EqualTo(class_def, 0) ThenBlock: {
                ReturnValue(0)
            }
            current_class = Hash.HGet(class_def, "parent")
        }

        ReturnValue(0)
    }
}

Function.JSOop.ClassGetFields {
    Input: class_name: Address
    Output: Address
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(Array.Create(0))
        }

        fields = Hash.HGet(class_def, "fields")
        ReturnValue(Hash.Keys(fields))
    }
}

Function.JSOop.ClassGetMethods {
    Input: class_name: Address
    Output: Address
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(Array.Create(0))
        }

        methods = Hash.HGet(class_def, "methods")
        ReturnValue(Hash.Keys(methods))
    }
}

Function.JSOop.ClassGetParent {
    Input: class_name: Address
    Output: Address
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }
        ReturnValue(Hash.HGet(class_def, "parent"))
    }
}

Function.JSOop.ClassExists {
    Input: class_name: Address
    Output: Integer
    Body: {
        JSOop.EnsureInit()

        class_def = Hash.HGet(JSClassRegistry.registry, class_name)
        IfCondition NotEqual(class_def, 0) ThenBlock: {
            ReturnValue(1)
        }
        ReturnValue(0)
    }
}


//=============================================================================
// SUPER CALL SUPPORT (prototype chain traversal)
//=============================================================================

Function.JSOop.SuperCall {
    Input: obj: Address
    Input: current_class: Address
    Input: method_name: Address
    Input: args: Address
    Output: Integer
    Body: {
        class_def = Hash.HGet(JSClassRegistry.registry, current_class)
        IfCondition EqualTo(class_def, 0) ThenBlock: {
            ReturnValue(0)
        }

        parent_class = Hash.HGet(class_def, "parent")
        IfCondition EqualTo(parent_class, 0) ThenBlock: {
            ReturnValue(0)
        }

        func_addr = JSOop._MethodLookupWithInheritance(parent_class, method_name)
        IfCondition EqualTo(func_addr, 0) ThenBlock: {
            ReturnValue(0)
        }

        num_args = Array.Size(args)

        IfCondition EqualTo(num_args, 0) ThenBlock: {
            result = CallIndirect(func_addr, obj)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 1) ThenBlock: {
            arg0 = Array.Get(args, 0)
            result = CallIndirect(func_addr, obj, arg0)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 2) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            result = CallIndirect(func_addr, obj, arg0, arg1)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 3) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            arg2 = Array.Get(args, 2)
            result = CallIndirect(func_addr, obj, arg0, arg1, arg2)
            ReturnValue(result)
        }

        IfCondition EqualTo(num_args, 4) ThenBlock: {
            arg0 = Array.Get(args, 0)
            arg1 = Array.Get(args, 1)
            arg2 = Array.Get(args, 2)
            arg3 = Array.Get(args, 3)
            result = CallIndirect(func_addr, obj, arg0, arg1, arg2, arg3)
            ReturnValue(result)
        }

        // 5+ args
        arg0 = Array.Get(args, 0)
        arg1 = Array.Get(args, 1)
        arg2 = Array.Get(args, 2)
        arg3 = Array.Get(args, 3)
        arg4 = Array.Get(args, 4)
        result = CallIndirect(func_addr, obj, arg0, arg1, arg2, arg3, arg4)
        ReturnValue(result)
    }
}


//=============================================================================
// END Library.JSOop.ailang
//=============================================================================
