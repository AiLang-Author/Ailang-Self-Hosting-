// Library.SyscallWrappersHaiku.ailang
// Auto-generated syscall wrappers for Haiku OS x86-64
// Generated by generate_syscall_lib_haiku.py
//
// Copyright (c) 2025 Sean Collins, 2 Paws Machine and Engineering. All rights reserved.
// Licensed under the Sean Collins Software License (SCSL).
//
// IMPORTANT NOTES:
// - Haiku syscall numbers are determined by ORDER in syscalls.h
// - Haiku _kern_read/_kern_write take a POSITION argument!
// - Haiku uses "teams" (processes), "areas" (memory regions), "ports" (IPC)
// - This file maps platform-neutral Sys* functions to Haiku _kern_* syscalls
//
// DO NOT EDIT MANUALLY - Regenerate with:
//   python3 generate_syscall_lib_haiku.py

LibraryImport.Compiler.Compile.Modules.CSyscallTableHaiku


// =============================================================================
// FILE I/O
// =============================================================================

// SysRead - Read from file descriptor
// Note: Haiku adds position arg; use -1 for current position
// Maps to: _kern_read (syscall 140)
Function.SysRead {
    Input: fd: Integer
    Input: buf: Address
    Input: count: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.READ, fd, -1, buf, count)
        ReturnValue(result)
    }
}

// SysWrite - Write to file descriptor
// Note: Haiku adds position arg; use -1 for current position
// Maps to: _kern_write (syscall 142)
Function.SysWrite {
    Input: fd: Integer
    Input: buf: Address
    Input: count: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.WRITE, fd, -1, buf, count)
        ReturnValue(result)
    }
}

// SysPread - Read at position
// Maps to: _kern_read (syscall 140)
Function.SysPread {
    Input: fd: Integer
    Input: buf: Address
    Input: count: Integer
    Input: offset: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.READ, fd, offset, buf, count)
        ReturnValue(result)
    }
}

// SysPwrite - Write at position
// Maps to: _kern_write (syscall 142)
Function.SysPwrite {
    Input: fd: Integer
    Input: buf: Address
    Input: count: Integer
    Input: offset: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.WRITE, fd, offset, buf, count)
        ReturnValue(result)
    }
}

// SysOpen - Open file
// Note: Haiku open takes dirfd as first arg; -1 for CWD
// Maps to: _kern_open (syscall 104)
Function.SysOpen {
    Input: path: Address
    Input: flags: Integer
    Input: mode: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.OPEN, -1, path, flags, mode)
        ReturnValue(result)
    }
}

// SysClose - Close file descriptor
// Maps to: _kern_close (syscall 149)
Function.SysClose {
    Input: fd: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.CLOSE, fd)
        ReturnValue(result)
    }
}

// SysLseek - Seek in file
// Maps to: _kern_seek (syscall 111)
Function.SysLseek {
    Input: fd: Integer
    Input: offset: Integer
    Input: whence: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SEEK, fd, offset, whence)
        ReturnValue(result)
    }
}

// SysFsync - Sync file to disk
// Note: Haiku takes dataOnly bool; 0=full sync
// Maps to: _kern_fsync (syscall 109)
Function.SysFsync {
    Input: fd: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.FSYNC, fd, 0)
        ReturnValue(result)
    }
}

// SysDup - Duplicate file descriptor
// Maps to: _kern_dup (syscall 150)
Function.SysDup {
    Input: oldfd: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.DUP, oldfd)
        ReturnValue(result)
    }
}

// SysDup2 - Duplicate fd to specific number
// Maps to: _kern_dup2 (syscall 151)
Function.SysDup2 {
    Input: oldfd: Integer
    Input: newfd: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.DUP2, oldfd, newfd, 0)
        ReturnValue(result)
    }
}


// =============================================================================
// DIRECTORY
// =============================================================================

// SysMkdir - Create directory
// Maps to: _kern_create_dir (syscall 113)
Function.SysMkdir {
    Input: path: Address
    Input: mode: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.CREATE_DIR, -1, path, mode)
        ReturnValue(result)
    }
}

// SysRmdir - Remove directory
// Maps to: _kern_remove_dir (syscall 114)
Function.SysRmdir {
    Input: path: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.REMOVE_DIR, -1, path)
        ReturnValue(result)
    }
}

// SysUnlink - Delete file
// Maps to: _kern_unlink (syscall 118)
Function.SysUnlink {
    Input: path: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.UNLINK, -1, path)
        ReturnValue(result)
    }
}

// SysRename - Rename file
// Maps to: _kern_rename (syscall 119)
Function.SysRename {
    Input: oldpath: Address
    Input: newpath: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.RENAME, -1, oldpath, -1, newpath)
        ReturnValue(result)
    }
}

// SysGetcwd - Get current working directory
// Maps to: _kern_getcwd (syscall 141)
Function.SysGetcwd {
    Input: buf: Address
    Input: size: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.GETCWD, buf, size)
        ReturnValue(result)
    }
}


// =============================================================================
// PROCESS (TEAM)
// =============================================================================

// SysExit - Exit process (team)
// Note: Haiku calls processes 'teams'
// Maps to: _kern_exit_team (syscall 41)
Function.SysExit {
    Input: code: Integer
    Body: {
        SystemCall(HaikuSysNum.EXIT_TEAM, code)
        // Does not return
    }
}

// SysExitGroup - Exit process (same as SysExit on Haiku)
// Maps to: _kern_exit_team (syscall 41)
Function.SysExitGroup {
    Input: code: Integer
    Body: {
        SystemCall(HaikuSysNum.EXIT_TEAM, code)
        // Does not return
    }
}

// SysFork - Fork process
// Maps to: _kern_fork (syscall 47)
Function.SysFork {
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.FORK)
        ReturnValue(result)
    }
}

// SysGetpid - Get process (team) ID
// Maps to: _kern_get_current_team (syscall 43)
Function.SysGetpid {
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.GET_CURRENT_TEAM)
        ReturnValue(result)
    }
}

// SysKill - Send signal to process
// Note: Haiku send_signal has more args
// Maps to: _kern_send_signal (syscall 97)
Function.SysKill {
    Input: pid: Integer
    Input: sig: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SEND_SIGNAL, pid, sig, 0, 0)
        ReturnValue(result)
    }
}


// =============================================================================
// SOCKET
// =============================================================================

// SysSocket - Create socket
// Maps to: _kern_socket (syscall 165)
Function.SysSocket {
    Input: domain: Integer
    Input: type: Integer
    Input: protocol: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SOCKET, domain, type, protocol)
        ReturnValue(result)
    }
}

// SysBind - Bind socket to address
// Maps to: _kern_bind (syscall 166)
Function.SysBind {
    Input: fd: Integer
    Input: addr: Address
    Input: addrlen: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.BIND, fd, addr, addrlen)
        ReturnValue(result)
    }
}

// SysListen - Listen on socket
// Maps to: _kern_listen (syscall 169)
Function.SysListen {
    Input: fd: Integer
    Input: backlog: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.LISTEN, fd, backlog)
        ReturnValue(result)
    }
}

// SysAccept - Accept connection
// Maps to: _kern_accept (syscall 170)
Function.SysAccept {
    Input: fd: Integer
    Input: addr: Address
    Input: addrlen: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.ACCEPT, fd, addr, addrlen, 0)
        ReturnValue(result)
    }
}

// SysConnect - Connect socket
// Maps to: _kern_connect (syscall 168)
Function.SysConnect {
    Input: fd: Integer
    Input: addr: Address
    Input: addrlen: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.CONNECT, fd, addr, addrlen)
        ReturnValue(result)
    }
}

// SysSend - Send data on socket
// Maps to: _kern_send (syscall 174)
Function.SysSend {
    Input: fd: Integer
    Input: buf: Address
    Input: len: Integer
    Input: flags: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SEND, fd, buf, len, flags)
        ReturnValue(result)
    }
}

// SysRecv - Receive data from socket
// Maps to: _kern_recv (syscall 171)
Function.SysRecv {
    Input: fd: Integer
    Input: buf: Address
    Input: len: Integer
    Input: flags: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.RECV, fd, buf, len, flags)
        ReturnValue(result)
    }
}


// =============================================================================
// MEMORY (AREAS)
// =============================================================================

// SysMmap - Map file to memory (via Haiku map_file)
// Note: Haiku uses areas; this maps through map_file syscall
// Maps to: _kern_map_file (syscall 220)
Function.SysMmap {
    Input: addr: Address
    Input: length: Integer
    Input: prot: Integer
    Input: flags: Integer
    Input: fd: Integer
    Input: offset: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.MAP_FILE, "mmap_region", addr, 0, length, prot, flags, 1, fd, offset)
        ReturnValue(result)
    }
}

// SysMunmap - Unmap memory region
// Maps to: _kern_unmap_memory (syscall 221)
Function.SysMunmap {
    Input: addr: Address
    Input: length: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.UNMAP_MEMORY, addr, length)
        ReturnValue(result)
    }
}

// SysMprotect - Change memory protection
// Maps to: _kern_set_memory_protection (syscall 222)
Function.SysMprotect {
    Input: addr: Address
    Input: length: Integer
    Input: prot: Integer
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SET_MEMORY_PROTECTION, addr, length, prot)
        ReturnValue(result)
    }
}


// =============================================================================
// TIME
// =============================================================================

// SysNanosleep - Sleep (via Haiku snooze_etc)
// Note: Haiku snooze_etc has different signature; wrapper needed
// Maps to: _kern_snooze_etc (syscall 196)
Function.SysNanosleep {
    Input: req: Address
    Input: rem: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.SNOOZE_ETC, 0, 0, 0, rem)
        ReturnValue(result)
    }
}


// =============================================================================
// IPC (PIPES/PORTS)
// =============================================================================

// SysPipe - Create pipe
// Maps to: _kern_create_pipe (syscall 121)
Function.SysPipe {
    Input: pipefd: Address
    Output: Integer
    Body: {
        result = SystemCall(HaikuSysNum.CREATE_PIPE, pipefd, 0)
        ReturnValue(result)
    }
}

