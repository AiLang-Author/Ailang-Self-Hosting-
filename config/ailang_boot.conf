# AILang OS Boot Configuration
# Signature: /etc/ailang_boot.conf.sig (verified when crypto layer is available)
#
# This file controls pre-database boot behavior.
# All post-boot configuration lives in PostgreSQL.

hostname=ailang-os
modules=auto
network=dhcp
wifi_auto=yes
login=yes
