# Workbench chrome — three commits, then layout/edit backlog

North star: `Design-Language- Refrence/Full desktop _ 1280_820.png` plus Shell / Pencil / Tracker crops. Haiku **slide-tab in one header strip**, dark glass, neon lip. Not Windows caption + toolbar.

Each of 1–3: implement → screenshot in guest → OK → **one git commit**. No bundling.

Close remains Alt+Q / Alt+F4 until a real chrome control exists. Magenta mark and cyan replicant wells wait until after the silhouette.

## Pass 1 — Tab silhouette

Stretchable body + shallow slant, lip only under the tab, Alteix title inside.

## Pass 2 — Drop Win32 caption buttons

Toolbar is app menu only (File / About / …). No `_ O X`.

## Pass 3 — Titlebar + rounded frame

Gradient titlebar tile; corner TVGs + edges; no clip-to-round yet.

## Later

Auckland layout / padding, document format, text editing, CAD chrome, theme editor.

Clipboard design (landed) lives in `docs/display/CLIPBOARD_SERVICE.md`, not `docs/plans/`.
