LibraryImport.Arena

LinkagePool.SimplePool {
    "x": Initialize=42
}

SubRoutine.Main {
    Arena_Init()
    ptr = AllocateLinkage(LinkagePool.SimplePool)
    val = ptr@x
    PrintNumber(val)
    PrintMessage("\n")
    Exit(0)
}
RunTask(Main)