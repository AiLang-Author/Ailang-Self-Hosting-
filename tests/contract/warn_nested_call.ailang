// Nested call as an argument — analyzer warning, compiler still accepts.
Function.Use {
    Input: n: Integer
    Output: Integer
    Body: {
        ReturnValue(n)
    }
}

SubRoutine.Main {
    x = Use(Add(1, 2))
    SystemCall(60, 0)
}

RunTask(Main)
