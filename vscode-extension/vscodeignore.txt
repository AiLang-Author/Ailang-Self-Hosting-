# .vscodeignore
# This is a BLACKLIST — everything NOT listed here gets packaged
# The extension needs: binaries, syntaxes, docs, snippets, configs

# Source control
.git/**
.gitignore

# Dev tooling
.vscode/**
node_modules/**
*.log
.eslintrc*
tsconfig.json

# Source files we don't ship (keep only compiled binaries)
*.ailang
!ailang.code-snippets
TestCode/**
Librarys/**

# Build artifacts we don't need
*.o
*.tmp

# OS junk
.DS_Store
Thumbs.db

# DO NOT EXCLUDE THESE (they must be in the VSIX):
# extension.js          — main entry point
# package.json          — manifest
# ailang_lsp.x          — LSP binary
# ailang.x              — compiler binary (optional, for compile/run)
# syntaxes/             — TextMate grammar
# docs/                 — manuals
# resources/            — icons
# language-configuration.json
# ailang.code-snippets
